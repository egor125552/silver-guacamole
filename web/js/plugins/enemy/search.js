import {distance,rand,clamp} from "../../core/math.js";import {randomTarget} from "./population.js";
function target(game,e,radius){for(let i=0;i<12;i++){const p={x:clamp(e.position.x+rand(-radius,radius),-game.settings.worldSize+1,game.settings.worldSize-1),z:clamp(e.position.z+rand(-radius,radius),-game.settings.worldSize+1,game.settings.worldSize-1)};if(!game.state.walls.some(w=>p.x>=w.x-.4&&p.x<=w.x+w.w+.4&&p.z>=w.z-.4&&p.z<=w.z+w.h+.4))return p;}return randomTarget(game);}
export default{id:"enemy-search",install(game){return game.on("update",({dt})=>{if(game.time<game.settings.gracePeriod)return;for(const e of game.state.enemies){if(!e.alive||game.time<e.stunnedUntil||e.state!=="searching")continue;const elapsed=game.time-e.stateSince;if(elapsed>18){e.state="patrolling";e.target=randomTarget(game);e.decisionAt=game.time+11;continue;}const d=distance(e.position,e.target);if(d>1.2){const moved=game.moveEnemy(e,{x:e.target.x-e.position.x,z:e.target.z-e.position.z},e.walkSpeed*(e.role==="investigator"?1.55:1.25),dt);game.emit("enemyMoved",{enemy:e,running:false,volume:70,moved});}else if(game.time>=e.decisionAt){e.target=target(game,e,Math.min(14,4+elapsed*.65));e.decisionAt=game.time+(e.role==="investigator"?.8:1.3);}}});}};
