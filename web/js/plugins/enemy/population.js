import {NPCType,AIState} from "../../core/constants.js";import {rand,distance,entityRect,rectIntersects} from "../../core/math.js";
function blocked(game,pos){const b=entityRect(pos,.5);return game.state.walls.some(w=>rectIntersects(b,w));}
export function randomTarget(game){const s=game.settings;return{x:rand(-s.worldSize,s.worldSize),z:rand(-s.worldSize,s.worldSize)};}
export function safeSpawn(game,min){const s=game.settings,p=game.state.player.position;for(let i=0;i<500;i++){const pos={x:rand(-s.worldSize+1,s.worldSize-1),z:rand(-s.worldSize+1,s.worldSize-1)};if(distance(pos,p)>=min&&!blocked(game,pos))return pos;}return{x:s.worldSize*.75,z:s.worldSize*.75};}
export default{id:"enemy-population",install(game){return game.on("reset",()=>{game.state.enemies=[];for(let i=0;i<20;i++){let type=i%4===0?NPCType.GUARD:NPCType.REGULAR;if(i===18)type=NPCType.SHOOTER;const l=game.enemyLoadout(type),e={id:i,type,position:safeSpawn(game,22),alive:true,health:l.maxHealth,maxHealth:l.maxHealth,weapon:l.weapon,behavior:l.behavior,state:AIState.PATROLLING,detection:0,hasReactedToDeath:false,target:null,waiting:false,decisionAt:game.time+rand(2,4),stateSince:game.time,nextStep:game.time,lastAttack:game.time,stunnedUntil:0,deathTime:-999,walkSpeed:game.settings.npcWalkSpeed,runSpeed:game.settings.npcRunSpeed};e.target=randomTarget(game);game.state.enemies.push(e);}});}};
