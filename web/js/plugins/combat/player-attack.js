import {Weapon} from "../../core/constants.js";import {distance} from "../../core/math.js";
const melee=new Set([Weapon.FIST,Weapon.MACHETE,Weapon.KNIFE,Weapon.CROWBAR,Weapon.BAT,Weapon.SHANK,Weapon.BATON]);
export default{id:"combat-player-attack",install(game){return game.on("playerAttack",()=>{const p=game.state.player;if(!p?.alive||game.time<p.stunnedUntil)return;const w=game.weaponProfile?.(p.weapon);if(!w||game.time-p.lastAttackTime<w.cooldown)return;p.lastAttackTime=game.time;let target=null,closest=w.range;for(const e of game.state.enemies){if(!e.alive||!game.hasLOS(p.position,e.position))continue;const d=distance(p.position,e.position);if(d<=closest){closest=d;target=e;}}game.audio.play(w.sound,{relative:true,volume:w.volume});if(!target){if(melee.has(p.weapon))game.audio.play("miss",{relative:true,volume:55});return;}if(p.weapon===Weapon.TASER)game.damageEnemy?.(target,0,{stun:true});else game.damageEnemy?.(target,w.damage);});}};
