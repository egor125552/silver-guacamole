function tone(ctx,frequency,seconds,decay,gain){const n=Math.max(1,Math.floor(ctx.sampleRate*seconds)),b=ctx.createBuffer(1,n,ctx.sampleRate),d=b.getChannelData(0);for(let i=0;i<n;i++){const t=i/ctx.sampleRate;d[i]=(gain/32768)*Math.sin(2*Math.PI*frequency*t)*Math.exp(-t*decay);}return b;}
function noiseTone(ctx,seconds){const n=Math.floor(ctx.sampleRate*seconds),b=ctx.createBuffer(1,n,ctx.sampleRate),d=b.getChannelData(0);for(let i=0;i<n;i++){const t=i/ctx.sampleRate,noise=Math.random()*2-1;d[i]=(23000/32768)*(Math.sin(2*Math.PI*1200*t)+.45*noise)*Math.exp(-t*15);}return b;}
function lowHealth(ctx){const n=ctx.sampleRate,b=ctx.createBuffer(1,n,ctx.sampleRate),d=b.getChannelData(0);for(let i=0;i<n;i++){const t=i/ctx.sampleRate,p=(Math.sin(2*Math.PI*2*t)+1)*.5;d[i]=(19000/32768)*Math.sin(2*Math.PI*300*t)*p;}return b;}
function shadow(ctx){const n=ctx.sampleRate,b=ctx.createBuffer(1,n,ctx.sampleRate),d=b.getChannelData(0);for(let i=0;i<n;i++)d[i]=(Math.random()-.5)*(3500/32768);return b;}
export default{id:"synthetic-cues",install(game){const a=game.audio,c=a.ctx;const defs={DetectionTick:[600,.10,30,15000],Spotted:[440,.45,3,26000],HealthIndicator:[880,.05,50,25000],MenuSelect:[700,.06,35,17000],MenuConfirm:[440,.10,25,21000],Dodge:[1050,.08,28,19000],Stun:[280,.22,8,26000],sonar_echo:[1200,.035,80,18000],EnemyPing:[980,.065,42,26000],ObjectivePing:[520,.12,18,22000],ObjectiveNear:[760,.09,25,23000],Unlock:[910,.24,8,25000],Locked:[210,.16,12,21000],Victory:[660,.65,2.4,27000],Wave:[360,.20,8,22000],Knife_Swish:[920,.08,24,17000],Machete_Swish:[520,.18,15,21000],Bat_Swish:[330,.16,14,20000],Blunt_Metal_Swish:[650,.13,20,19000]};for(const [name,args] of Object.entries(defs))if(!a.buffers.has(name))a.setBuffer(name,tone(c,...args));a.setBuffer("LowHealth",lowHealth(c));a.setBuffer("Shadow_Ambience",shadow(c));a.setBuffer("Taser_Fire",noiseTone(c,.25));}};
